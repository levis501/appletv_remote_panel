ISSUES_001.md


1. Investigate the scale of the hitboxes and the background remote image. it seems that the farther down the page, the more the hitboxes are below where they should be.  As if the vertical scale of the hitboxes is larger.  There may also be a horizontal scale issue, although given the narrow width of the image, it is difficult to know for sure.

2. Change the install script to default the "Would you like to configure devices now?" to N if there are any known devices.

3. On the d-pad, let double-clicks on left or right correspond to skip back and skip forward, respectively.

4. Create a rounded-rectangle button for each app selected, and stack them below the lowest hitboxes.  When the extension is first installed, default the following apps to be selected TV, Music, Settings, Youtube, Netflix, Hulu.  Customize each button with one of the following, in order of highest priority to lowest:
- An icon for the app bundled with the extension
- An icon for the app downloaded from the internet after installation 
- A button with the color-scheme for the app icon, and the name of the app
- A black button with the name of the app in white letters, scaled to ensure that the entire name fits in the button
